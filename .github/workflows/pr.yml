name: C/C++ CI

on: [pull_request]

jobs:
  build:

    runs-on: [self-hosted, windows]
    
    steps:
    - uses: actions/checkout@v1
      
    - name: Setup MSBuild.exe
      working-directory: Build
      shell: cmd
      run: startenv.bat     
